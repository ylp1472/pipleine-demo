name: ci

on:
  push:
    branches: main

jobs:
  login:
    runs-on: ubuntu-latest
    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: nerurodeomo
          password: X=2d?cc=:>6EaMu

      # - name: Build the Docker image
      #   run: docker build -t nerurodeomo/coursework-group_14:latest .

      # - name: Push to DockerHub
      #   run: docker push nerurodeomo/coursework-group_14:latest
